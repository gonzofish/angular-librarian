/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { {{ className }} } from './{{ filename }}.pipe';

describe('{{ className }}', () => {
    it('', () => {
        const pipe = new {{ className }}();
        expect(pipe).toBeTruthy();
    });
});
